#==== Default server ===========================================================

server {
	server_name localhost;
	root /www/public;
	include vhost.conf;

	# Optional: Enable status pages for health monitoring tools such Netdata
	include status.conf;
}
